app:
  name: "ONT Tester NEXUS"
  version: "0.1.0"
  environment: "development"

monitor:
  heartbeat_interval_s: 1.0
  poll_interval_s: 1.0
  disconnect_threshold: 3
  ping_timeout_ms: 500

workers:
  max_workers: 24
  worker_join_timeout_s: 5.0

ui:
  enabled: true
  refresh_interval_ms: 500

logging:
  level: "INFO"
  logs_dir: "data/logs"

database:
  enabled: true
  path: "data/runtime/nexus.db"
  init_on_startup: true

selenium:
  browser: "chrome"
  headless: true
  implicit_wait_s: 2
  page_load_timeout_s: 20
  script_timeout_s: 20
  window_width: 1400
  window_height: 900
  chrome_binary_path: "tools/browser/chrome-win64/chrome.exe"
  chromedriver_path: "tools/browser/chromedriver-win64/chromedriver.exe"

auto_execution:
  enabled: true
  trigger_on_connect: true